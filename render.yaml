services:
  - type: web
    name: hesabu-api
    runtime: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "PYTHONPATH=. alembic -c server/migrations/alembic.ini upgrade head && PYTHONPATH=. gunicorn server.wsgi:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: hesabu-db
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: ENV
        value: production
      - key: JWT_COOKIE_SECURE
        value: "true"
      - key: JWT_COOKIE_SAMESITE
        value: "None"
      # The frontend authenticates with a Bearer token on every mutating request
      # (see client/src/lib/api.ts) rather than relying on the cookie, so the
      # double-submit CSRF cookie protection is both unreadable cross-origin
      # (document.cookie can't see another origin's cookie) and redundant here.
      - key: JWT_COOKIE_CSRF_PROTECT
        value: "false"
      - key: PYTHON_VERSION
        value: "3.12.7"
      # Comma-separated, no trailing slash. Note the "-sigma" suffix: plain
      # hesabu.vercel.app belongs to an unrelated project, so Vercel assigned
      # this one. Getting this wrong silently breaks every browser request.
      - key: CORS_ORIGINS
        value: "https://hesabu-sigma.vercel.app"

databases:
  - name: hesabu-db
    plan: free
    databaseName: hesabu
    user: hesabu
